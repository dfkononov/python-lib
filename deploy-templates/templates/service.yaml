apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-3-8-import-def-github.fullname" . }}
  labels:
    {{- include "python-3-8-import-def-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-3-8-import-def-github.selectorLabels" . | nindent 4 }}
